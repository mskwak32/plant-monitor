# 11주차 맥락 — 웹 대시보드 완성: Chart.js 그래프, 임계값 설정 폼, 펌프 이력 화면

## 현재 진행 상황

- [ ] Chart.js 센서 그래프 — 토양 수분 / 온도 / 습도 시계열 그래프
- [ ] 임계값 설정 폼 — 웹에서 입력 후 `POST /settings/threshold` 호출
- [ ] 펌프 이력 화면 — `GET /pump/logs` 결과를 목록으로 표시

---

## 이번 주 배운 것들

